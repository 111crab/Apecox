# 当前代码设计

更新日期：2026-08-05

当前没有已批准的 Gameplay C++ 实施任务，因此没有待创建的类、成员变量、函数或 GameplayTag。

进入 Phase 1 后，本文件将先列出 Gameplay Framework 的候选类名、父类、职责、公开成员和生命周期，由用户审阅后再生成子代理 Prompt。

所有获批的新类都必须在设计表中同时标注对称路径：`.h` 位于 `Source/Apecox/Public/<领域>/...`，`.cpp` 位于 `Source/Apecox/Private/<领域>/...`。
